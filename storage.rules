rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Open storage rules for chat audio, songs, voice notes & images
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
